services:
  sales-agent:
    build: .
    image: joseph-sales-agent:latest
    container_name: joseph-sales-agent
    restart: unless-stopped
    ports:
      - "3004:3004"
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
